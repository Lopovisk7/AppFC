## Packages
framer-motion | For smooth animations (card flips, transitions)
lucide-react | Icon library (already in base stack, but emphasizing usage)
file-saver | For saving generated Anki export files
clsx | Utility for conditional classes
tailwind-merge | Utility for merging tailwind classes

## Notes
Tailwind Config - extend fontFamily:
fontFamily: {
  sans: ["'DM Sans'", "sans-serif"],
  display: ["'Outfit'", "sans-serif"],
  mono: ["'JetBrains Mono'", "monospace"],
}
